{
  "name": "noctis-wifi-planner",
  "version": "1.0.0",
  "description": "NOCTIS WiFi coverage planner — single-page app for placing access points, walls, and dead zones on floor plans.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^22.1.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
